Local TSN cases copied into the deliverable live here.

Use:

```bash
python3 -m group9_network_scheduling import-cases ../tsn-test-cases-main/test_cases
python3 -m group9_network_scheduling list-cases
```

Each imported case directory should contain:

- `topology.json`
- `streams.json`
- `routes.json`
- optional `WCRTs.csv`
